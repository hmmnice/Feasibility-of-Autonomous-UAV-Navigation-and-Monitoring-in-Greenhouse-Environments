# greenhouse_inspection

ROS 2 package for the location-driven greenhouse inspection pipeline.

The package provides the Matplotlib crop selector, static greenhouse map,
automatic viewpoint search, structured and A* routes, preflight validation,
continuous multi-view execution and image capture.

Build and run instructions are in the submission's top-level `README.md`.
